name: Build MacOSX
on:
  push:
    branches:
      - master
      - AddMacOSX

jobs:
  build:
    runs-on: macOS-latest
    steps:
    - uses: actions/checkout@v3
    - id: INSTALL_OPENSSL
      run: brew install openssl
    - id: INSTALL_FUSE
      run: brew install --cask fuse
    - id: LIST_INSTALLS
      run: ls -l /usr/local/opt
    - id: LIST_CASKS
      run: ls -l /usr/local/Caskroom/fuse
    - id: CONFIGURE
      run: ./configure --with-openssl=/usr/local/opt/openssl --with-fuse=/usr/local/Caskroom/fuse
    - id: PrintSpec
      run: cat /Users/runner/work/beak/beak/build/x86_64-apple-darwin21.6.0/spec.mk
    - id: MAKE
      run: make VERBOSE=
    - id: TEST
      run: make test
